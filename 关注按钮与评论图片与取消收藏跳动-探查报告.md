# 关注按钮 · 评论图片输入 · 取消收藏跳动 — 探查报告

> 2026-09-22 · 受限实测 + 代码核对 · 范围按当日收窄：关注流不做、他人粉丝/关注列表不需要；只评估「主页关注/取关按钮」「评论图片输入」「取消收藏跳动」三件事。

## 结论速览

| # | 需求 | 结论 | 一句话依据 |
|---|---|---|---|
| 1 | 主页关注/取关按钮 | ✅ **可行且改动小，建议做** | `POST/DELETE api/v2/follows` 实测 201/200；模型与主页 UI 都已具备关系态 |
| 2 | 评论输入框图片输入 | ❌ **建议不做**（相册选图不可行，外链版 UX 差） | 服务端无任何上传端点；评论 media 接受外链已实测，但用户得自己找图链 |
| 3 | 取消收藏窗口跳动 | ✅ **根因明确，修复方案已定** | 就地删除无滚动补偿；列表现有锚定机制可直接复用 |

---

## 1. 关注 / 取关按钮（✅ 建议做）

### 接口实测（2026-09-22，探针写操作均已清理并复核无残留）

| 操作 | 结果 |
|---|---|
| `POST api/v2/follows` body `{user_id: 全0假uuid}` | 404 JSON `user.not_found` ⇒ 路由与参数名成立 |
| `POST api/v2/follows` body `{user_id: 自己}` | **HTTP 201**，响应体 = 被关注用户的资料对象 |
| `DELETE api/v2/follows?user_id=自己` | **HTTP 200**，响应体 = 用户资料对象 |
| `GET v2/follows/followings` 复核 | 无自关注残留 ✅ |

要点：
- **服务端不拒绝自关注**（对自己的 id 也返回 201）→ 客户端必须在自己的主页隐藏按钮（is_self 判定）。
- 客户端现状已就位大半：`UserProfile` 已解析 `is_following / is_follower / is_friend / is_muted / is_blocked`（models.dart:782-786）；`user_profile_page.dart:304-311` 已渲染「已关注 / 互相关注 / 关注了你 / 已拉黑 / 已静音」关系 chips。
- 边界：
  - 他人的粉丝/关注列表拿不到（`follows/followers?user_id=` 被服务端忽略，四需求报告已证）——本需求不需要。
  - 关注流端点本次新探明：`GET api/v2/posts/followings?last_id=&per_page=10` 返回 200（8.6KB 真实数据）。按本次范围**不做**，仅记录备查。
  - `is_disable_follow`（对方关闭了关注）模型未解析；先按请求失败 Snackbar 兜底，后续有需要再补字段。

### 改动面（预估小）
- `simple_api.dart`：加 `followUser / unfollowUser` 两个方法（POST/DELETE，响应即用户对象）。
- `user_profile_page.dart`：关系 chips 区加「关注 / 已关注」按钮 + 乐观更新（与点赞同一模式）；自己的主页隐藏。
- 无需新状态层。

---

## 2. 评论输入框图片输入（❌ 建议不做）

结论先行：**用户预期的「从相册选图发评论」不可行**；唯一通的是「粘贴图片外链」，交互别扭，随「太麻烦就算了」一并砍掉。

证据链：
1. 评论 API 原生接受 media 图片项：实测 `POST api/v2/comments` 带 `media:[{type:"image", url:外链}]` → **HTTP 201**，响应回显 media；测试评论已删除、复核无残留。
2. 客户端渲染已通用：`post_detail_page.dart:759-780` 对 `comment.media` 走 `MediaThumb`（56dp 缩略图，点击进查看器、长按保存）——任意 url 都能显示，外链无需改渲染。
3. 但**服务端没有任何上传端点**：v2/v3 的 `uploads / media / attachments / files / upload_tokens / qiniu_tokens` 七个候选 GET 全部 404 HTML（Rails 错误页，非应用级 JSON）；官方 Web 产物 `main.dart.js` 中 upload/七牛零痕迹。
4. 官方 Android App 有上传能力（权限文案「上传或拍摄图片、视频」），但其上传端点不在 Web 面——要拿到只能抓包官方 App，成本高且随版本可变。

若将来改主意做「外链版」：评论输入框加图片链接入口 → 待发媒体列表（`pending_emoji_strip` 已有同类 UI 可参照）→ 发送时并入 media。改动很小，但体验有限。

---

## 3. 取消收藏后窗口跳动（✅ 根因明确，待实施修复）

### 现象复现链（对照用户日志）
1. `17:26:29.908` 正文展开 → 条目 `67c8610b…` 279→2573px（Δ+2294）
2. `17:26:36.159` `DELETE /api/v2/favourites` → 200 → 「取消收藏成功」
3. `removeFavourite → paged_list.removePost`：**就地删除该条 + 重建列表 + 重算「上次浏览到这儿」分界**（日志：分界定位在第 1 条）
4. 全程无任何滚动补偿 → 视口内容整体跳变

### 根因
列表组件已有完整的**高度变化锚定机制**（`paged_post_list.dart:167-188`：`_anchorId / _anchorTop / _anchorOffset / _anchorTargetTop` + `_restoreAnchor`，服务于展开全文/收起/整条折叠）。但它由「高度将变」通知驱动；取消收藏走的是**整条删除**路径，且被删的恰恰是当前展开的那条大高度条目——锚点条目本身消失，补偿无从谈起。

### 修复设计（实施阶段再做，本次不动代码）
- 取消收藏删除前，以**被删条目的内容坐标顶**为不变量记锚（`_anchorOffset + _anchorTop` 的既有口径，锚被回收场景已有同款处理先例）。
- `removePost` 重建后，后继条目恰好落在被删条目原内容顶位置 → 校正滚动把该位置对回视口原处。效果：**被删条目下方的内容纹丝不动**，只有它自己从列表里消失。
- 只用真实布局定位、不用估计 Δ——与补页补偿「弃估计值」的既有结论一致；若单帧可见闪动，可套用现有 `_freezeFrame` 定格帧方案。
- 次要注意：删除后分界线重算（分界定位在第 1 条）不要在补偿落地帧并发改动滚动。

---

## 附：探查留痕
- 脚本：`analysis/probe_3needs_live.py`、`analysis/probe_3needs_live2.py`；输出 `*_out.txt`。
- 纪律：串行 ≥1.25s、浏览器 UA、Authorization=裸 token、不碰 /auths、输出不含 token。
- 写探针全部清理并复核无残留：测试评论（发→删→复核）、自关注（关注→取关→复核）。
