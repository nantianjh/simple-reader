# 应用体积剖析与内建浏览器方案 - 探查报告

> 调查对象：`Simple 搜索 v1.7.1`（`app/pubspec.yaml:5`），Android / Flutter
> 样本：`D:\debug\A-anduoid Project\simplesousuo\dist\Simple搜索-v1.7.1-release.apk`（实测 50.16 MB）
> 方法：按 zip 条目逐项解析 APK（release 包），未做任何估算性猜测。
> 日期：2026-09-17

---

## 一、50 MB 的构成：不是依赖冗余，是 Flutter 引擎 × 3 个 ABI

### 1.1 实测体积构成

| 分组 | 压缩后大小 | 内容 |
|---|---|---|
| lib/x86_64 | **18.32 MB** | libflutter.so 12.45 MB + libapp.so 5.88 MB |
| lib/arm64-v8a | **16.89 MB** | libflutter.so 11.20 MB + libapp.so 5.69 MB |
| lib/armeabi-v7a | **14.50 MB** | libflutter.so 8.22 MB + libapp.so 6.28 MB |
| classes*.dex | 0.24 MB | 全部 Java/Kotlin 代码 |
| assets/ | 0.11 MB | flutter_assets（NOTICES、图标字体、shader） |
| res/ + resources.arsc | 0.01 MB | 系统资源 |
| 其余（manifest 等） | < 0.03 MB | — |
| **合计** | **50.16 MB** | |

关键事实：

- **三个 ABI 的 native 库合计 49.7 MB，占 APK 的 99%**。
- `libflutter.so` = Flutter 引擎（渲染 + Dart VM），是 Flutter 应用的固定地基，任何 Flutter 应用都甩不掉；单个 ABI 约 8–12 MB。
- `libapp.so` = Dart AOT 编译产物。**空 Flutter 项目也约 5–6 MB**（这是 Dart VM 快照的基础结构），本项目 50 个 Dart 文件的业务代码实际增量预计不足 1 MB。
- 业务侧真正的"冗余"接近于零：`pubspec.yaml:11-24` 的零依赖策略生效——dex 仅 236 KB，资源合计不到 300 KB。

### 1.2 因果链

```
flutter build apk 默认行为 = fat APK
  → 一次打包 armeabi-v7a + arm64-v8a + x86_64 三份引擎
  → 每份 = 完整 libflutter.so + 完整 libapp.so
  → 18.32 + 16.89 + 14.50 ≈ 49.7 MB
  → 真机安装时只会加载其中一份（你的手机是 arm64）
```

即：**50 MB ≈ 1 份引擎 + 1 份你的代码，被完整复制了 3 遍**。x86_64 那份（18.3 MB）几乎只有模拟器用得上。

补充：`.so` 在 APK 内以未压缩、对齐方式存储（本报告压缩前后大小完全一致即为证据），这是现代 AGP 的默认（`useLegacyPackaging=false`），安装时直接 mmap、不另复制，所以"系统设置里看到的应用占用"与 APK 大小基本相当——两端口径都会显示 ~50 MB。

### 1.3 优化路径对比

| 方案 | 做法 | 预期体积 | 代价/风险 |
|---|---|---|---|
| **A. `abiFilters` 锁定 arm64（推荐）** | `android/app/build.gradle.kts` 的 `defaultConfig` 内加 `ndk { abiFilters += listOf("arm64-v8a") }` | **~17.2 MB（-66%）** | 模拟器（x86_64）无法安装；日常构建命令不变 |
| B. `--split-per-abi` | `flutter build apk --release --split-per-abi` | arm64 单包 ~17.2 MB | 产物分 3 个文件；versionCode 会按 ABI 偏移（`build.gradle.kts:24-29` 已注释此行为），分发略繁琐 |
| C. 单 ABI 构建参数 | `flutter build apk --target-platform android-arm64` | ~17.2 MB | 依赖命令行 flag，易忘记 |
| D. AAB | `flutter build appbundle` | 按设备下发 | 自装需 bundletool 转包，流程麻烦，不适合 sideload |
| E. 开启 R8/shrinkResources | `build.gradle.kts` release 块加 `isMinifyEnabled` | 收益 **< 0.1 MB** | dex 仅 236 KB、资源 7 KB，无可收缩空间；反而引入构建不确定性。**不建议** |

结论：**选 A**。改动仅 1 行，arm64 是当前所有国产真机的标准 ABI（armeabi-v7a 机器 2019 年后已基本停产）。若偶尔需要模拟器调试，临时删掉该行即可。

### 1.4 减完之后还剩什么

优化后的 ~17.2 MB 中：Flutter 引擎 11.2 MB（不可减）+ Dart AOT 产物 5.7 MB（业务代码仅占其中约 1 MB）+ 资源 0.3 MB。这是 Flutter 技术栈的地板价，若想压到 5 MB 以内只能换技术栈（如 KMP/原生），对一个"零依赖、克制"的项目没有必建议。

---

## 二、内建浏览器方案：内核在系统里，APK 可以零增量

### 2.1 先破除一个误区

"内建浏览器 = 塞内核"只在桌面端/特殊场景成立。Android 上：

- **系统 WebView 是操作系统组件**（Android 5.0+ 由 ROM/Play 服务以独立 APK 持续更新），应用调用 `android.webkit.WebView` 时用的是系统那份 Chromium，**APK 内不含任何浏览器内核**；
- 真正会打包内核的是 GeckoView（~80 MB）或内嵌 Chromium（~150 MB+）这类自选内核方案——对打开链接的场景没有任何理由使用，直接排除。

因此"内建浏览器"在本项目上的体积代价可以做到**接近 0**。

### 2.2 方案对比

| 方案 | APK 增量 | 体验 | 与工程约定的契合 |
|---|---|---|---|
| **A. 自建系统 WebView 桥（推荐）** | **~0.05–0.1 MB**（纯 dex） | 应用内可返回、可关闭；首次冷启动稍慢 | 完全契合：延续 `store/files/logs/system` 四通道模式，pub 依赖仍为 0 |
| B. Chrome Custom Tabs（androidx.browser） | ~0.1–0.3 MB | 最佳（进程常驻秒开、共享登录态），但依赖设备装有 Chrome/Edge，国内 ROM 参差 | gradle 加 1 个 Google 官方包；pub 侧仍为 0 |
| C. webview_flutter（官方 pub 插件） | ~0.5–1 MB | 同 A，且支持 JS 交互/进度回调 | **破坏 `pubspec.yaml:11-24` 的零依赖铁律**，并引入插件注册链 |
| D. 维持现状：外部浏览器（`MainActivity.kt:294` 的 ACTION_VIEW） | 0 | 跳出应用 | 现状即此，成本为零 |

### 2.3 推荐方案 A 的落地设计

复用既有桥接模式（`MainActivity.kt:14-47` 的通道注释、`lib/platform/native_bridge.dart:19-31` 的约定），改动四处：

1. **新增原生 `BrowserActivity.kt`**（与 `MainActivity.kt` 同目录）：
   - 内含一个系统 `WebView`，顶部简条（标题 + 关闭按钮），接收 `url` extra；
   - 返回键：`webView.canGoBack() ? goBack() : finish()`；
   - 通过 Intent extra 传入主题色/深浅色，与 Flutter 侧主题一致。
2. **`MainActivity.kt` 的 `handleSystem`（286-310 行）新增方法 `openInApp { url }`**：`startActivity(Intent(this, BrowserActivity::class.java).putExtra("url", url))`。
3. **`AndroidManifest.xml` 注册 Activity**：`<activity android:name=".BrowserActivity" android:exported="false" .../>`（INTERNET 权限已具备，`AndroidManifest.xml:4`）。
4. **Dart 侧 `NativeBridge.openInApp(url)`**（`native_bridge.dart:216-226` 旁新增），并在设置里加"链接打开方式：应用内 / 系统浏览器"开关（`lib/data/settings.dart` 已有键值存储），调用点在 `link_chip.dart` / `linkified_text.dart` / `actions.dart` 现用 `openUrl` 处分流。

体核算账：以方案 A + 体积优化叠加计算——

| | 现状 | 优化后 |
|---|---|---|
| APK | 50.16 MB | **~17.3 MB**（arm64 单 ABI + WebView 桥） |
| pub 依赖 | 0 | 0（不变） |
| 浏览器内核 | 无 | 无（用系统 WebView，由系统持续更新，安全性更好） |

### 2.4 注意事项

- WebView 渲染发生在原生 Activity 层，不在 Flutter widget 树内；与"页内一半是网页"的 PlatformView 形态相比，这种"整页原生"方式实现简单、无混合滚动问题，代价是网页界面与 Flutter 界面之间是整页切换。对"点开正文链接"场景足够。
- 系统 WebView 因 ROM 而异（华为设备通常预置华为版 WebView 实现，API 一致），无需做厂商适配，但个别老旧 ROM 的 WebView 可能缺少新 JS 特性——属于可接受退化。
- Custom Tabs 与自建 WebView 不互斥：可先探测支持 Custom Tabs 的浏览器，有则走 B、无则走 A，增量同 A。个人自用项目建议先只做 A，观察体验再决定是否补 B。

---

## 三、行动建议（按优先级）

1. 【1 行改动】`android/app/build.gradle.kts` `defaultConfig` 加 `ndk { abiFilters += listOf("arm64-v8a") }` → 体积 50.16 → ~17.2 MB。
2. 【约 200 行】自建 `browser` 通道 + `BrowserActivity`，做"应用内打开链接"设置项，默认保持现状（系统浏览器）。
3. 【不建议】开启 R8/shrinkResources：收益趋近于零。
4. 【排除】任何打包浏览器内核（GeckoView 等）的路线。
