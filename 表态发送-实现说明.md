# 点赞「表态」发送 — 实现说明（长按点赞按钮）

> 2026-09-23 · 落点：**主工作树** `C:\dev\projects\simple-reader`（`app/lib/`）
> 依据《点赞状态承载能力-探查报告.md》。**未提交、未构建**（按你的要求）。**未实现「被你逗笑了」（`laugh`）**。
>
> 本轮按你的三条反馈改：① 送出后**不弹 toast**；② 普通点赞**同意走 v3**；③ 表态条**改成原位展开**（不再是底部弹层），落在主工作树。
> 上一版在独立工作树 `Worktrees\simple-reader\vote-state-3468528`（用的是底部弹层），已被本次取代；**该工作树里还留着那版代码**，确认不要了就删（`git worktree remove`）。

---

## 1. 需求 ↔ 代码

| 你说的 | 实现 |
|---|---|
| 长按点赞按钮出现待选实际发送状态 | 长按点赞按钮 → **在按钮栏正下方原位展开一条表态条**，10 个胶囊：表情 + 短名（🤝 理解 / 🌱 支持 / 💪 加油 / 🫂 安慰 / 🌤️ 祝福 / 🤍 共鸣 / 🌙 陪伴 / 🌸 温暖 / ✨ 看见 / 💐 感谢）；每项长按可看服务端那整句文案（Tooltip，如「🫂轻轻安慰了你」） |
| 再次点击状态发送指定状态 | 点某一项 → 条自动收起 + 立刻 `POST api/v3/votes {post_id, vote_type}`；按钮回显成「表情 + 短名」（🫂 安慰）。**成功不弹 toast**（结果就在按钮上） |
| （隐含）长按不改状态 | 长按只开/关这条，不动点赞态；再长按一次收起 = 什么都没发生；未赞的动态选一项 = 带态度赞一下 |

10 种（去掉了 `laugh`）：`understanding` / `support` / `encouragement` / `comfort` / `blessing` / `resonance` / `accompany` / `warmth` / `see` / `gratitude`。

## 2. 改到的文件（9 个，其中新增 2 个）

| 文件 | 改动 |
|---|---|
| `data/vote_states.dart` | **新增**。状态表（id / 表情 / 服务端文案 / 按钮短名）+ `byId()`；`plain='unknown'` 单列，写明"维护约定"与它作为参数的风险 |
| `ui/widgets/vote_state_bar.dart` | **新增**。表态条（原位展开的那一行）。只负责"画 + 回调选中的 id"，**不做网络、不弹层、不碰 Overlay、无动画** |
| `api/simple_api.dart` | `vote()` 加可选 `voteType`；**点赞改走 v3**，取消仍走 v2 DELETE |
| `ui/actions.dart` | `toggleVote` 落覆盖层时改记表态；**新增 `sendVoteState()`**（乐观更新 + 失败回滚 + 400 友好提示）；成功**不弹提示** |
| `data/vote_overlay.dart` | 动态侧 `Map<String,bool>` → `Map<String,String>`（值 = `vote_type`），旧数据 `true/false` 兼容读入；新增同步读 `postVoteType()` |
| `ui/widgets/post_card.dart` | 点赞按钮接 `onLongPress` → 展开/收起条；选中 → 送出 → 回显（`_actionItem` 支持用表情替掉心形）；条与快捷评论条互斥 |
| `ui/post_detail_page.dart` | 详情页点赞按钮同上（同一组件、同一口径） |
| `data/backup.dart` | 仅 schema 文档：补 `voteOverlay.posts` 值的形态与新旧的兼容说明（导出/导入本来就走原始 JSON，无代码改动） |
| `data/changelog.dart` | 1.9.7 里加一条用户可感知变化 |

仅本功能的差异（含两个新文件全文，可直接 `git apply -p1`）：`表态发送-仅本次改动.diff`。

## 3. 几个刻意的取舍（理由都写在代码注释里）

1. **普通点赞从 v2 换到 v3（不带 `vote_type`）** —— 唯一改到老路径的地方，按你这次的确认保留。原因：服务端取消是"软删"，v2 再赞会把上一次的态度带回来；v3 不带参数才会**复位**成普通赞。取消仍走 `DELETE api/v2/votes`（实测跨版本能删）。想退回原样只改 `simple_api.vote()` 一处。
2. **表态条用"原位展开"，不用弹层** —— 复用工程里已经跑通的那套：`_voteBarOpen` 字段 + Column 里 `if (_voteBarOpen) VoteStateBar(...)`，与「快捷评论条」（`_composerOpen` / `_inlineComposer`）**同构**；展开前先走列表锚点 `PagedPostListAnchor`，所以卡片撑高时下方内容不会跳。**不引入 Overlay / bottom sheet / 菜单定位**这套新机制 —— 这是本机风险最小的形式。代价：卡片会临时变高一条（评论条本来也这样）。
3. **条与评论条互斥** —— 两条同时摊开会让卡片一下子高出一大截，在这套对高度变化敏感的列表里不划算；展开一条就收起另一条。
4. **成功不弹 toast，失败仍提示** —— 成功的反馈就是按钮上的表情 + 短名（切条即见，不打断浏览）；失败不能静默（网络错、token 失效、400 白名单过期各有对应提示，"静默失败"比"多弹一条"糟得多）。**若你要的是连失败都不弹，说一声，删 `sendVoteState` 里那几行即可。**
5. **态度只本机记，不主动拉 `v3/votes` 列表** —— 读侧拿不到"我这条是什么状态"（动态对象只有 `is_voted` 布尔），按报告第六节建议不做每帖一次请求。代价说清楚：按钮上的回显只认**本机送出的那一次**，别的设备/官方 App 改过不会同步。
6. **文案表硬编码** —— 服务端不下发清单，表是实测并集（10 项），有"服务端增删"的风险：遇到 400 提示「这个态度服务端不认了，先换一个吧」，不崩、也不静默退化成普通赞。
7. **评论点赞不动** —— 评论侧没有读接口（405），报告结论是"不能断言支持"，保持二值。
8. **`postVoteType()` 是同步纯内存读** —— 覆盖层在内容解析阶段就已载入，卡片渲染时一定拿得到，不必异步。

## 4. 基线说明（**重要**）

改动落在**主工作树**，所以它是"主工作树当前全部未提交改动 + 本功能"的叠加态：叠加前我做了完整快照，`git status` 里那 9 个文件之外的部分**一个字没碰**（含取消收藏方案 B 的 `paged_post_list.dart`、备注名相关的 `user_remarks.dart` / `remark_name.dart` 等）。

想单独看本功能：`表态发送-仅本次改动.diff`（基线 = 本次改动前的主工作树）。

## 5. 复测路径（本次没构建，所以未实测）

构建照旧：`app/build_arm64.cmd`（或 `flutter build apk --release --target-platform android-arm64`）。

- **未赞动态**：长按点赞 → 条展开 → 点「🫂 安慰」→ 条收起、按钮变「🫂 安慰」、**没有 toast**；退出重进（含缓存续读）仍显示 🫂（覆盖层生效）。
- **已赞动态**：长按换「🤍 共鸣」→ 按钮变「🤍 共鸣」。想核实服务端：`GET api/v3/votes?post_id=<你的帖>` 找到自己那条（按 `created_at` 倒序，自己那条不一定在第一页）。
- **条的展开/收起**：长按开、再长按关；卡片撑高时下方内容**不该跳**（锚点日志：`展开/收起表态条：<postId>`）；先点开评论条再长按点赞 = 评论条自动收起。
- **老路径没坏**：单击点赞/取消；取消后再单击点赞 = 普通赞（v3 复位），不会把老态度带回来。
- **失败路径**：断网 / token 失效（走"去配置"提示）/ 400（提示换一个）。
- **详情页**同卡片口径；详情页展开条会推动下方内容（页面级滚动，与评论表情面板同性质）。

## 6. 未做 / 可加

- 「被你逗笑了」按你的要求不收进表（服务端认这个值，表里加一行就能开）。
- **没有"回到普通赞"的入口**：现在只能取消点赞再单击。要一个「普通赞」选项的话，条里加一项、`sendVoteState` 传 `null` 即可（v3 不带参数即复位）。
- 条的**对齐**目前与卡片内容左对齐；想让条贴着点赞按钮那一侧（右置按钮组的场景）需要接 `AppSettings.interactionButtonsOnRight`，本轮没做。
- 没加测试：本功能是 UI + 网络，纯逻辑只有覆盖层的旧数据兼容可测（`test/` 现有用例的写法可照抄）。
- **静态分析没跑成**：本机 dart.exe 孵化子进程管道坏（OS error 231，项目 `MEMORY.md` 已记），只能 `dart format --output=none` 做语法解析校验（9 个文件零解析错误），类型检查要等构建。
