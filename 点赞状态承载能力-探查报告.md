# 点赞「其它状态」承载能力 — 探查报告

> 2026-09-22 · 离线静态提取（官方 Web 产物 + 官方 App `libapp.so`）+ 受限在线实测
> **结论：能。v2 只支持二值点赞；「其它状态」的唯一入口是 `POST api/v3/votes` + `vote_type`，服务端做白名单校验，已实测确认 11 个非默认状态。**

---

## 结论速览

| # | 问题 | 结论 |
|---|---|---|
| 1 | 点赞能否发送其它状态 | ✅ **能**。`POST api/v3/votes` body 加 `vote_type`，服务端**认**并落库 |
| 2 | 为什么官方网页版看不到 | ✅ 与你的判断一致：**Web 包里 `vote_type` 零痕迹**，且 Web 侧本就有「web版暂不支持，请去App操作」文案；**官方 App 才走 v3** |
| 3 | 是不是「可能在 v3」 | ✅ **确认在 v3**。官方 App `libapp.so` 的 198 个接口**全是 `api/v3/…`**，`api/v2/votes` 在 App 里根本不存在 |
| 4 | 状态全集 | 服务端**白名单校验**（非法值 400）。实测命中 **11 个非默认状态** + 默认态 `unknown`（见第二节表） |
| 5 | 怎么知道写进去了 | 唯一读侧：`GET api/v3/votes?post_id=` 能看到每条投票记录的 `vote_type`；v2 侧同名端点**没有该字段** |

---

## 一、写入契约（实测矩阵，逐步读回校验）

目标只用**自己的动态**；每步行进中读回两次防列表陈旧；结束复核无残留。

| 步骤 | 请求 | 结果 | 读回的 `vote_type` |
|---|---|---|---|
| 1 | `POST v2/votes {post_id}` | 201 | `unknown`（赞了你的动态） |
| 2 | `POST v2/votes {post_id, vote_type:"comfort"}` | 201 | **`unknown`** ← v2 **忽略** vote_type |
| 3 | `POST v3/votes {post_id, vote_type:"comfort"}` | 201 | **`comfort`** 🫂轻轻安慰了你 ← **v3 生效** |
| 4 | `POST v2/votes {post_id}`（已赞态再点） | 201 | `comfort` ← v2 不动已有状态 |
| 5 | `POST v3/votes {post_id}`（不带参数） | 201 | `unknown` ← **v3 不带参数会复位** |
| 6 | `POST v3/votes {post_id, vote_type:"resonance"}` | 201 | `resonance` 🤍共鸣了你的动态 ← **可改状态** |
| 7 | `POST v3/votes {post_id, vote_type:"bad_value"}` | **400** | `resonance` ← 非法值被拒、原状态不动 |
| 9 | `POST v3/votes {post_id, vote_type:"support"}`（未赞态直接带状态） | 201 | `support` 🌱向你表达了支持 |

非法值的错误体（不含合法值清单）：

```json
HTTP 400  {"error":"vote_type does not have a valid value"}
```

其余要点：
- **大小写/空格敏感**：`Understanding`、`"understanding "`、`""` 全部 400。
- **兼容**：`POST v3/votes` 建出来的状态，用 `DELETE v2/votes` 能正常取消（跨版本删除通）。
- **删除是"软删"**：取消后再普通点赞，会保留**上一次的 `vote_type`** 与**首次点赞时间**（证据：矩阵第 10 步取消后，下一次普通 POST 读回的是老的 `support` 而不是 `unknown`；该记录的 `created_at` 停留在历史上的 2026-08-03）。
- `vote_type` 只在写侧存在；**动态对象本身只暴露 `is_voted` 布尔**，读不到"我这条是什么状态"。

---

## 二、状态全集（服务端白名单，实测命中 11 个非默认值）

`vote_type_name` 由**服务端下发**（App 包里搜不到这些中文，说明文案不在客户端）:

| `vote_type` | 服务端下发文案 | 来源 |
|---|---|---|
| *（不传 / v2）* → `unknown` | 赞了你的动态 | 基线 |
| `understanding` | 🤝表示理解你的感受 | 字典探测 + 真实数据 |
| `support` | 🌱向你表达了支持 | 字典探测 + 真实数据 |
| `encouragement` | 💪给你加油鼓劲了 | 字典探测 |
| `comfort` | 🫂轻轻安慰了你 | 字典探测 + 真实数据 |
| `blessing` | 🌤向你表达了祝福 | 字典探测 |
| `resonance` | 🤍共鸣了你的动态 | 字典探测 + 真实数据 |
| `accompany` | 🌙正在静静陪伴你 | 字典探测 |
| `warmth` | 🌸被你温暖了 | 字典探测 |
| `see` | ✨看见了你的表达 | **真实数据采样发现**（未在字典里） |
| `gratitude` | 💐谢谢了你 | 字典探测 |
| `laugh` | 😂被你逗笑了 | 字典探测 |

- ⚠️ **不保证已穷尽**：白名单无法枚举，以上是「字典探测（约 86 个候选词，3 轮）+ 真实数据采样（自己动态 124 条投票记录）」的并集。`see` 就不在任何字典里、只从真实数据里捞到——说明**继续扩大采样仍可能发现新值**。
- 真实数据里的分布（自己 8 条动态、124 条记录）：`unknown` 占绝大多数，非默认值合计 4 种（`see` / `support` / `understanding` / `resonance` / `comfort`），**是少数用户的主动行为**。

---

## 三、读侧结构（`GET api/v3/votes`）

```
GET api/v3/votes?post_id=<动态id>&last_id=&per_page=10
→ 200 裸数组；不带 post_id → 400 {"error":"post_id is missing"}
              post_id 不存在 → 404 {"code":404,...post.not_found}
```

单项结构（**投票人对象 + 投票状态**，不是投票记录）：

```jsonc
{
  "id": "<用户 id>",                 // ← 找自己那条靠它
  "vote_type": "understanding",      // 仅 v3 有
  "vote_type_name": "🤝表示理解你的感受",  // 文案由服务端下发
  "created_at": "2026-08-03T…",      // ⚠️ 首次点赞时间，不是用户注册时间也不是本次点赞时间
  "nickname": "…", "avatar_url": "…", "avatar_color": "…", "gender": "…",
  "is_official": false, "is_privacy_enabled": false, "is_hide_gender_age": false,
  "is_following": false, "is_follower": false, "is_blocked": false,
  "is_disable_follow": false, "is_inactive": false, "is_locked": false,
  "can_private_message": false,
  "iap_avatar": …, "iap_avatar_accessory": …,
  "moment": { … }                     // 部分记录才有
}
```

- **列表按 `created_at` 倒序**：我本次新点的赞**不会排到第一页第一条**，而是停在历史位置（因为该字段是首次点赞时间）。做「谁赞了」列表时要注意。
- `v2/votes?post_id=` 同路径仍可用，但**只有 8 个字段**（纯用户对象），**没有 `vote_type`**。

---

## 四、为什么官方 Web 没有、App 有（静态证据）

**官方 Web 产物（`.workbuddy/cache/web-bundle/main.dart.js`，4.34MB）**

| 检索项 | 命中 |
|---|---|
| `api/v2/votes` | 5（GET 列表 + POST/DELETE 二值） |
| `api/v3/votes` | **0** |
| `vote_type` / `vote_type_name` | **0** |
| 评论区点赞 `api/v2/comment_votes` | 3（POST/DELETE，body 仅 `comment_id`） |

Web 侧点赞写法（`Sn(a)`）：

```js
q.l(0,"post_id",k.ax.gA(0))          // body 只有 post_id
if(已赞) A.dh(..+"api/v2/votes",..)   // POST
else     A.eE(..+"api/v2/votes",..)   // DELETE
```

并且 Web 包里本就有「App 专属」文案常量：

```js
s($,"crE","apX",()=>A.v("web版暂不支持，请去App操作"))
```

**官方 App（`simple-1.9.26-base.apk` → `lib/arm64-v8a/libapp.so`，24.5MB）**

| 检索项 | 命中 |
|---|---|
| `api/v2/votes` | **0** |
| `api/v3/votes` | 3 |
| `api/v3/comment_votes` | 1 |
| `vote_type` | 2 |
| `vote_type_name` | 1 |
| `understanding` | 1 |
| 全部接口串 | **198 个，清一色 `api/v3/…`** |

App 侧相关线索：`extended_like_bar.dart`（点赞条可扩展）、`likeFirstFlag`（首次点赞提示，对应 Web 的「点赞数仅作者可见，真实无压力！」只弹一次）、`see_votes_count`。

---

## 五、顺带新探明的 v3 端点（本次首次记录）

来自 App 接口表 + 在线验证（均为只读 GET）：

| 端点 | 实测 | 说明 |
|---|---|---|
| `GET api/v3/votes/posts?post_id=` | 200，13.5KB | 返回**动态对象**数组（与点赞人相关的内容流） |
| `GET api/v3/votes/voted_posts?last_id=&per_page=10` | 200，11.3KB | **我点赞过的动态**列表 |
| `GET api/v3/moments/emotions` | 200，18.3KB | 心情动态列表（含 `emotion_tag`，如 `peaceful`） |
| `GET api/v3/posts/summaries/my_emotions` | 200 | `{"emotions":["struggle","thoughtful",…]}` |
| `GET api/v3/configs` | 200，**23 字节** | 只有 `{"init_page_index":…}`，**页面配置不在服务端** |

App 接口表里另有 `api/v3/moment_votes`、`api/v3/cover_votes`、`api/v3/vlog_votes`、`api/v3/user_iap_product_votes`、`api/v3/comment_votes` —— 说明"投票/表态"是平台级概念，评论、封面、vlog 各有对应写端点。

---

## 六、落到我们客户端（建议，本次不动代码）

1. **状态能力**：把 `simple_api.dart:vote()` 扩成可选状态参数，走 v3：
   ```dart
   Future<void> vote({required String postId, required bool on,
                      String? voteType, required String token})
   // on && voteType != null → POST v3 {post_id, vote_type}
   // on && voteType == null → POST v3 {post_id}   （会复位成普通赞）
   // !on → DELETE v2/v3 {post_id}
   ```
   注意：**必须用 v3**，v2 会静默忽略 `vote_type`（这是最容易踩的坑）。
2. **UI 形态**：官方是「点赞条可扩展」（`extended_like_bar`）+ 服务端下发文案 —— 我们做的话，长按/展开出状态菜单，**选中项文案直接用服务端的 `vote_type_name`**，避免硬编码 emoji 与译法漂移。
3. **状态回显**：动态对象只有 `is_voted`，拿不到"我这条是什么状态"。要显示就得 `GET v3/votes?post_id=` 找自己那条 —— **代价不小（每帖一次请求 + 分页）**，且列表按首次点赞时间排序、自己那条不保证在第一页。建议：**只记住自己刚设的状态（本地覆盖层）**，不主动拉取。
4. **本地点赞覆盖层**（`data/vote_overlay.dart`）目前是 `Map<String,bool>`；若要支持状态，建议换成 `Map<String,String?>`（值 = vote_type，null = 未赞），向后兼容旧数据（`true→"unknown"`，`false→删除`）。
5. **风险**：`vote_type` 是**未公开的白名单**，官方可能增删；未知值一律 400，按"状态不支持"提示即可，不要崩。

---

## 七、未决 / 需注意

1. **枚举未穷尽**：11 个非默认值是实测并集，非官方清单。要拿到确切全集，只能抓官方 App 的真实请求或继续扩大真实数据采样。
2. **评论点赞是否也支持状态**：`v2|v3/comment_votes` 的**读侧 405**（无读接口），写侧 `+vote_type` 返回 201 但**无从读回校验**，故**不能断言**评论侧也支持。
3. **删除语义**：证据指向"软删/行复用"（再次普通点赞会保留上一次的 `vote_type` 与首次点赞时间）。若我们做状态 UI，需要处理"取消后重赞会回到旧状态"这一现象。
4. **`created_at` 不是点赞时间**：`v3/votes` 列表里没有"本次点赞时间"，也没有"谁在什么时候赞的"。

---

## 附：探查留痕

**脚本（`analysis/`）**

| 脚本 | 用途 |
|---|---|
| `probe_bundle_vote.py` / `vote2.py` / `vote3.py` / `vote4.py` | 官方 Web 产物的点赞面静态提取（含中文 `\uXXXX` 转义检索） |
| `probe_apk_vote.py` / `apk_vote2.py` | 官方 App `libapp.so`：端点枚举 + `vote_type` 邻域 |
| `probe_vote_state_live.py` | 端点盘点 + 附加参数是否被采纳 |
| `probe_votes_v3_live.py` | v3/votes 结构、必需参数、负面基准 |
| `probe_vote_write_live.py` | 首次证明「v2 忽略 / v3 生效」 |
| `probe_vote_enum_live.py` | 枚举边界（400 校验）+ 兄弟端点存在性 |
| `probe_vote_options_live.py` | 选项表来源（configs / emotions） |
| `probe_vote_fulllist_live.py` | 一轮字典 + 真实数据采样 |
| `probe_vote_final_live.py` | 二轮字典 + 深层采样 + 覆盖语义 |
| `probe_vote_matrix_live.py` | **写入契约矩阵（最权威）** |
| 输出 `*_out.txt` 与之一一对应 | |

**纪律**：串行 ≥1.25s、浏览器 UA、`Authorization`=裸 token、不碰 `/auths`、无并发、无全量抓取、输出不含 token。

**写入探针安全声明**

- 全部写操作**只作用于我自己的动态**（`6e89d1c4-…`），单帖、可逆（点赞/取消）。
- 每次探测后立即取消并复核；**结束态：`is_voted=False`、`votes_count=44`、`v3/votes` 中无己方记录** —— 与起始态一致。
- 字典探测的非法值命中 **400，不产生任何状态变化**，无需清理。

**本轮网络请求总量**：向 `simple.imsummer.cn` 共 **约 348 次**带凭证请求（含分批探测的重复运行），其中写操作约 **90 次**（点赞/取消配对，均在同一帖上，已全部回滚）。
