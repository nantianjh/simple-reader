# 长图自动缩放错位 — 修复计划

> 现象：长图点开后**自动放大已生效，但画面整体甩到屏幕右侧**——左侧全黑、右侧内容被屏幕右缘裁掉近 3/4（截图见会话，图左缘 ≈ 屏宽 74% 处）。
> 范围：仅 `app/lib/ui/image_viewer_page.dart` 单文件。本计划只做分析，未改任何代码。

---

## 1. 结论先行

上一轮修的是「**根本没缩放**」（服务端 width/height=0 → 自动缩放整体失效），已用真实解码尺寸（`_resolveIntrinsicSize`）修好；这一轮暴露的是**另一个 bug**：自动缩放的矩阵只给了纯缩放、没给配套平移，而 Flutter `Transform` 默认以**视口左上角**为缩放锚点，导致居中放置的长图放大后整体右移出屏。同时横向钳制逻辑把 dx 钉死在 0，用户单指/双指都**拖不回来**。

## 2. 根因分析（几何推导）

### 2.1 变换模型

`_ZoomableImage.build`（L453-468）：

```
Transform(transform: _transform.value, child: SizedBox.expand(child: Center(child: NetImage(fit: contain))))
```

* `Transform` 未传 `origin`/`alignment`。已核对本机 Flutter SDK（`C:\dev\flutter`，`rendering/proxy_box.dart` `_effectiveTransform`）：两者均为 null 时**直接返回原始矩阵**，即缩放以子盒子（= 视口大小的 SizedBox）**左上角 (0,0)** 为锚点。
* 子树内 `BoxFit.contain` 的长图渲染条在盒内**水平居中**。

### 2.2 错位量推导

设视口宽 `V`、高 `H`，长图自动缩放倍率 `s`（铺满宽度公式 `(图高/图宽)÷(H/V)`）。contain 渲染条：

* 条宽 `w0 = V/s`，条左缘 `L0 = (V − V/s)/2 = V(s−1)/2s`，条高 = H、顶缘 = 0。

纯缩放矩阵（`_maybeAutoScale` L350-351：`Matrix4.identity()..scaleByDouble(s,s,s,1)`，**平移 = 0**）以左上角为锚点作用后：

* 图左缘 = `s·L0 = V(s−1)/2 > 0` → **整体右移 `V(s−1)/2`**，图右侧 `(s−1)/s` 比例被裁出屏。
* 纵向：条顶缘 `s·0 = 0` → 顶对齐，恰好正常。

**与截图互相印证**：图左缘在屏宽 74% 处 → `V(s−1)/2 = 0.74V` → `s ≈ 2.48`；再反推图宽高比 ≈ 4.7:1（拼接聊天长图的典型比例），全部吻合。横向错、纵向对，也解释了截图里"顶到底、右边缺、左边黑"的形态。

### 2.3 为什么用户无法自救

* `_autoScaleOnly` 档钳制（L419-422）把 **dx 恒置 0**（`Offset(0, …)`）：当初自动缩放的平移本来就是 0，单指上下浏览时横向被钉死在错位位置。
* 用户双指捏合后退出 autoScaleOnly，普通档横向范围 `[−V(t−1), 0]`（L423-427）同样是按"图左缘在 0"的假设推导的，**不含 `L0` 修正**——真实的可覆盖范围应是 `[V − t(L0+w0), −t·L0]`。两条钳制都建立在同一个错误几何假设上，错位状态无法被任何手势纠正。

### 2.4 顺带发现的潜伏 bug（同根因，一并修）

普通**竖图**（contain 只撑满宽度、高不足）渲染条纵向也居中（`top0 = (H−h0)/2 > 0`）。纵向钳制 `[−H(t−1), 0]` 同样假设顶缘在 0 —— 放大后把图拖到边缘会露出**上下黑边**。只是平时焦点锚定把图带回去、不易察觉；长图因初始平移固定为 0 而彻底暴露。

## 3. 修复方案（方案 A：几何感知的初始平移 + 钳制）

保持现有手势架构（PinchGestureRecognizer、焦点锚定、锁翻页口径）全部不动，只修矩阵与钳制。

### 改动点 1：`_maybeAutoScale` 补上配套平移

应用自动缩放时写完整矩阵 `T(tx, ty)·S(s)`：

* `tx = −s·L0 = −V(s−1)/2`（图左缘回到 0，水平铺满）
* `ty = 0`（顶对齐，长图从顶部开始读）

等价几何表述：水平方向按"图片中心"缩放、纵向按"顶缘"缩放。改动约 3 行 + 注释。

### 改动点 2：`_clampTranslation` 重写为几何感知

在 state 里由 `_intrinsicSize`（兜底 `_metaSize`）+ `_viewport` 算出渲染条矩形 `(L0, top0, w0, h0)`（contain 的绘制矩形），对缩放量 `t`：

* **横向**：
  * `t·w0 ≥ V`（盖得住）：`tx ∈ [V − t(L0+w0), −t·L0]`
  * 盖不住：回中 `tx = V/2 − t(L0 + w0/2)`
* **纵向**（把 top0 纳入，替换现有两处写死 0 的逻辑）：
  * `t·h0 ≥ H`（盖得住）：`ty ∈ [H − t(top0+h0), −t·top0]`
  * 盖不住：回中 `ty = H/2 − t(top0 + h0/2)`
* **`_autoScaleOnly` 特例可整体删除**：`t = s` 时横向范围退化为单点 `{−V(s−1)/2}`（下界 = 上界，代入可验证恰好等于改动 1 的 tx），"横向钉死"由退化区间自然保证；纵向公式代入 top0=0、h0=H 后与现有 `[−H(t−1), 0]` 完全一致，行为不变。
* 浮点防护：区间上下界倒挂（|差| < 0.5px）时取中点，避免 clamp 抖动。
* `_intrinsicSize` 未就绪（手势极少发生）时保留现行为兜底。

### 改动点 3：焦点锚定公式确认不动

`_onScaleStart/_onScaleUpdate` 的 `translation = focal − childPoint·t` 对任意 `T(tx,ty)·S(t)` 结构普遍成立（纯矩阵代数，与渲染条位置无关），**不需要改**。弹回动画、`_lockByIndex`、`_autoScaleOnly` 的"上报 false 不锁翻页"语义均不动。

### 明确否决的捷径

* **`Transform(alignment: Alignment.center)` 一行修**：水平确实回正，但纵向变成中心锚点 → 长图打开直接定位到**图片中部**（顶部在屏外），且与纵向钳制的"顶缘在 0"假设冲突，等于换个方向错。
* **整体换 `photo_view` 包**：本项目手势是踩过 InteractiveViewer 与 PageView 竞技场冲突的坑后手工定制的（见文件头注释），换包风险大于收益，否决。

### 可选加固（低优先级）

服务端下发了 width/height 但**错误**的图：meta 抢跑会按错误比例定 s。可加：intrinsic 到手后若宽高比与 meta 偏差 > 10% 且用户尚未做手势 → 重算一次初始矩阵。极长图（真实 s > ceiling 20）盖不住宽度时，几何感知钳制会自动改为**居中呈现**（比现在"甩出屏幕"好），无需额外处理。

## 4. 验证清单（修完后按此复测）

| # | 用例 | 预期 |
|---|------|------|
| 1 | 打开长图（约 5:1、10:1、>20:1 各一） | 铺满宽度、顶对齐、无左右黑边；>20:1 居中呈现 |
| 2 | 长图单指上下滑 | 纵向平移正常，横向纹丝不动，不误翻页 |
| 3 | 长图双指捏合放大 → 拖动 → 捏回 1 | 首次捏合不再"先缩小"（上限跟随 _appliedAutoScale）；缩回弹回原尺寸并恢复翻页 |
| 4 | 普通竖图捏合放大后拖到上/下边缘 | 不出现上下黑边（潜伏 bug 修复验证） |
| 5 | 横图 / 方图捏合 | 行为与现状一致（横向范围公式在 L0=0 时退化为现值） |
| 6 | 多图帖子左右翻页 | 各页变换互不影响，放大页锁翻页、缩回解锁 |
| 7 | 尺寸字段为 0 的图 | 打开无异常，等待解码后自动缩放生效 |

## 5. 实施步骤

1. 修改 `image_viewer_page.dart`（改动点 1、2，及注释同步；预计净增 ~30 行）。
2. `flutter analyze` 清零。
3. 按第 4 节清单真机复测。
4. git 提交 → 重建 APK → 面向用户的变更记录补一条。

## 6. 风险评估

* 改动集中在一个纯数学函数 + 一个矩阵初始化点，无状态机/手势结构调整，回归面小。
* 钳制公式在普通图（L0=0、top0=0 时）与现行为完全一致，普通图路径理论上零回归。
* 唯一行为变化：长图打开位置从"错位甩右"变为"顶对齐铺满"——即本修复目标本身。
