# 取消收藏删除补偿失效 — 日志核对报告

> 2026-09-23 · 依据 2026-09-22 23:01–23:02 实机日志 + 源码核对 · 只核对不改代码。
> 日志背景：白天探查报告《关注按钮与评论图片与取消收藏跳动-探查报告.md》第 3 节给出「取消收藏跳动」修复设计；当晚 23:01 的这份日志已是**补偿机制落地后的复测**（日志中已出现「删除条目预跳 / 删除后残差校正」字样，对应 `paged_post_list.dart` 的 `anchorItemRemoval` / `_restoreAfterRemoval`）。**复测结论：跳动没有修复。**

## 一、核心异常：删除补偿净效果为零，等效于无补偿就地删除

### 日志中的两次取消收藏（同一种失效模式）

| 时刻 | 被删条目 | 预跳（删除前） | 残差校正（删除落地后） | 净滚动变化 |
|---|---|---|---|---|
| 23:01:55 | c7b15ed4…（高 345.3px） | 滚动 2911.9 → **2566.6**（−345.3） | 滚动 2566.6 → **2911.9**（+345.3） | **0** |
| 23:02:46 | 44df1e3b…（高 435.6px） | 滚动 5323.9 → **4888.3**（−435.6） | 滚动 4888.3 → **5323.9**（+435.4） | **0** |

参考条目对账（校正日志里的「视口位置 −85.2 → 260.1」「−221.5 → 214.1」）：

- 参考条目 42f2686b：删除前视口顶 **−85.2**（顶在视口上方 85.2px）；预跳把它推到 −85.2+345.3 = **260.1**；被删条目在它**下方**，删除不影响它 → 实测 after 恰为 260.1；残差 delta = 260.1−(−85.2) = **+345.3 = 预跳量**。
- 参考条目 2da136d9：同理，−221.5 → 214.1，delta = **+435.6 = 预跳量**。

两案例 delta 与 shift **完全相等**并非巧合，是代码结构下的数学必然。

### 根因（paged_post_list.dart 871–984 行）

实现的两步：

1. **预跳**：`target = scroll.offset − shift`（shift = 后继顶 − 被删顶的实测内容坐标差），意图是「删除后让**被删条目下方**内容纹丝不动」；
2. **残差校正** `_restoreAfterRemoval`：不变量是「**视口最靠上的可见条目**（`tracker.firstVisibleKey`）回到它**删除前**的视口位置」。

矛盾点：参考条目选取用的是 `firstVisibleKey`，判定条件是「条目底部越过列表顶 ≥1px」（read_tracker.dart:82），**顶在视口上方的条目也会入选**，且按列表序取第一条 —— 于是取消收藏一个视口内的条目时，参考条目几乎必然是**被删条目的前驱**（只露出底部一条缝的那条）。前驱位于被删条目上方：

- 预跳让它视口 y 增大 shift（被推下去）；
- 删除对上方条目无影响；
- 校正按「回到删除前位置」把滚动 +shift → **预跳被完整撤销**。

净效果 = 滚动零变化 = 被删条目顶以下内容整体上移一个条目高度 = **与不做任何补偿的就地删除完全等效**，即原始「取消收藏跳动」bug 原样保留。两个案例的参考条目 refTopBefore 均为负值（−85.2 / −221.5，顶在视口上方），是「参考条目在被删条目上方」的直接证据。

该机制唯一能正确工作的情形是被删条目**整体已划出视口顶**（此时参考条目在其下方，delta=0，预跳保留）——恰好与用户实际操作（删的是眼前这条）相反。探查报告原设计是「以**被删条目的内容坐标顶**为不变量」，实现走样成了「以**视口最靠上可见条目**为不变量」，二者在参考条目位于被删条目上方时互斥。

已排除的干扰项：两次 jumpTo 均无「jumpTo 未按预期落地」探针警告（`_jumpWithProbe`），可排除「偏移被同步覆盖」；`_removalAppliedOffset` 安全阀未触发（两次校正都执行了）。

## 二、次要观察（非 bug，记录在案）

**刷新血脉重建后，补页立刻命中旧血脉的响应缓存**（23:02:41）：刷新宣告「旧游标链 4 格作废、重建 2 格（深度 1 页）」后仅 6 秒，补第 2、3 页直接「来源=本地缓存」命中 `||89d84fae…|10`、`||44df1e3b…|10` —— 这两个键本次会话从未落盘，是历史会话的响应缓存。

机制上不算错误：响应缓存键 =（端点, last_id, per_page），不含血脉代次；新血脉第 1 页末游标与旧会话相同（收藏列表头部未变）→ 同参数请求命中旧响应；第 2 页衔接由游标链保证（last_id 相同 ⇒ 服务端切片位置相同）。这是 preferCache 在线补页的设计内行为（paged_list.dart:510 注释明示「联网模式缺缓存直接抓取，属正常浏览路径」）。但与「存档血脉重建」在用户感知上有落差：列表深层内容已变时，刷新后的下滚会展示旧页（preferCache 固有取舍），后续若做「刷新后强制新鲜翻页」可再议。

## 三、自洽项核对（排除清单，均无问题）

- 「旧游标链 **4** 格作废，重建为 **2** 格」：格 = 游标链长度（含首页空游标），刷新前 3 页 = 4 格、重抓 1 页 = 2 格（reading_positions.dart:310 `cursors.length`）✓
- 「记录上次浏览内容 **29** 条」：3 页 30 条 − 已删 1 条（c7b15ed4）✓
- 分界移动逻辑：删 c7b15ed4（位于分界之后）分界保持第 2 条 ✓；删 ecee254a（第 1 条，分界之前）分界 2→1 ✓
- 23:02:37 取消收藏 ecee254a 无预跳/校正日志：当时滚动在顶部 0，`clamp(0, max)` 后位移 <0.5px 按设计不打日志（903/965 行）✓
- 网络纪律：users→posts/profile 间隔约 1.2s 串行 ✓；收藏/取消收藏 201/200 全部成功，无 401/5xx ✓
- 补页「补上一页跳过：无上一页可补」为双向补页的顶部边界日志，正常 ✓

## 四、修复方向建议（待确认后实施，本次未动代码）

1. **方案 A（最小改动）**：`_restoreAfterRemoval` 中判定参考条目若位于被删条目上方（删除前 `offsetOfKey` 可比较，或复用已记录的被删条目顶），直接放弃校正、保留预跳 —— 保持「被删条目下方内容不动」口径。
2. **方案 B（回归原探查设计）**：不变量改为「被删条目**后继**顶回到被删条目原内容顶」，即参考条目取后继而非视口顶；预跳准确时残差≈0，仅剩分界条位移需要补齐。
3. 无论选哪个，`_maxRemovalRounds` 的二次校正需同步约束，避免分界重算在校正后再次引入漂移；顶部删除（滚动=0）路径维持现状即可。
