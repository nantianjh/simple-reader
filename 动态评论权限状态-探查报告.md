# 动态「是否允许评论」状态 — 探查报告

> 2026-09-22 · 离线产物静态提取 + 受限在线实测 · 结论：**该状态可以拉到，且服务端已下发两个现成字段；500 是"无校验直发"才触发的服务端裸异常。**

## 结论速览

| # | 问题 | 结论 |
|---|---|---|
| 1 | 能否拉取某条动态「是否允许评论」的状态 | ✅ **能，且不需要探测式试错**。服务端下发 `comment_permission`（字符串枚举）与 `can_comment`（布尔） |
| 2 | 不允许评论时服务端是否返回 500 | ⚠️ **未能在受控条件下复现**（样本极稀有 + 单账号无法以非作者身份施加），但获得了更可靠的替代答案：官方客户端在 `can_comment=false` 时**根本不发请求**，所以 500 只会发生在"绕过校验直发"的客户端上 |
| 3 | 客户端当前状态 | `comment_permission` 已解析但**全项目零使用**；`can_comment` 未解析；发评论前无任何校验，500 原样透传给用户 |

---

## 一、字段：服务端已下发状态，无需试探

### 1.1 `comment_permission` 的三值枚举（离线静态证据）

官方 Web 产物 `main.dart.js` 的常量表定义了完整的评论权限选项（line 159800）：

```js
s($,"coE","bHt",()=>{var q=t.N
return [A.at(["name","public_comments",     "value",$.bYu()],q,q),   // 所有人
        A.at(["name","followings_comments", "value",$.bYv()],q,q),   // 我关注的人
        A.at(["name","no_comments",         "value",$.bYw()],q,q)]})  // 禁止别人评论
```

对应文案常量（line 159998-160002）：

| 常量 | 文案 |
|---|---|
| `bYt` | 谁可以评论 |
| `bYu` | 所有人 |
| `bYv` | 我关注的人 |
| `bYw` | 禁止别人评论 |
| `bBO` | **作者已开启禁止评论** ← 官方在禁止评论时给用户的提示语 |

即 `comment_permission ∈ {public_comments, followings_comments, no_comments}`。

### 1.2 `can_comment` —— 比枚举更适合直接使用的布尔

Post 模型的 `fromJson` / `toJson` 同时解析两个字段（line 42026 / 42066）：

```js
c=$.Z().K(a8.i(b1,"comment_permission"),a9)
if(c!=null)a7.at=c
b=$.Z().K(a8.i(b1,"can_comment"),s)          // 布尔
if(b!=null)a7.ax=b
```

### 1.3 官方客户端的判定方式：本地拦截，不发请求（line 143113）

```js
ME(a,b,c){var s=this,r=s.ax
  r===$&&A.d()
  if(!r.gIb()){A.c9($.bBO());return}   // can_comment=false → 弹「作者已开启禁止评论」并 return
  ...向 api/v2/comments 发 POST
```

同文件另有评论入口的文案分支 `a.gIb() ? $.bY9() : $.bBO()`（line 39221 / 142656），即**整个评论入口都是按 `can_comment` 切换形态的**。

### 1.4 写侧路径（供后续参考）

厂商前端改评论权限走选择框，选中项的 `value` 直接 PUT 到单条动态（line 146653）：

```js
q.at = p                                    // post.comment_permission = 选项 value
s.l(0,"comment_permission",p)
A.fq(A.au()+"api/v2/posts/"+q.gA(0), r.d, s, ...)   // PUT api/v2/posts/{id}
```

---

## 二、端点覆盖实测（2026-09-22）

| 端点 | `comment_permission` | `can_comment` | 备注 |
|---|---|---|---|
| `GET api/v3/posts/search`（列表） | ✅ 有 | ❌ 无 | 抽样 240 条，0 条含 `can_comment` |
| `GET api/v2/posts/mine`（列表） | ✅ 有 | ❌ 无 | 10 条 |
| `GET api/v2/posts/profile`（列表） | ✅ 有 | ❌ 无 | 抽样 46 条 |
| **`GET api/v2/posts/{id}`** | ✅ 有 | ✅ **有** | **本次新探明**；HTTP 200 对象 |
| **`GET api/v3/posts/{id}`** | ✅ 有 | ✅ **有** | **本次新探明**；字段更多 |

**单条动态端点（新探明，此前契约只记录了 v3 搜索与 v2 列表端点）**

```
GET api/v2/posts/{id} → 200 对象
 键集：can_comment, comment_permission, comments_count, content, created_at, id,
       is_disable_download, is_disable_forwarding, is_favourited, is_owner, is_pinned,
       is_reviewing, is_show, is_timed_post, is_voted, post_type, user, visibility, votes_count
     can_comment=True  comment_permission='public_comments'

GET api/v3/posts/{id} → 200 对象（多出 show_type / view_count / tags / is_thanked /
       is_collection_pinned / has_excluded_users）

负面基准：不存在的 id → 404 JSON
  {"code":404,"message":"Translation missing: zh-CN.errors.post.not_found","debug_info":""}
（v2/v3 行为一致）
```

**结论性差异**：`can_comment` **只在单条详情下发**。列表端点（搜索/主页/自己的动态）**都没有**它，只有 `comment_permission`。这决定了客户端的取数策略（见第四节）。

---

## 三、500 的成因判断

### 3.1 采样结果：非默认状态极稀有

| 采样面 | 条数 | `comment_permission` 分布 |
|---|---|---|
| v3 搜索（12 关键词×2 页） | 240 | public_comments 240 |
| 他人主页流（6 用户） | 46 | public_comments 46 |
| 自己的动态 | 10 | public_comments 10 |
| **合计** | **296** | **public_comments 296，非默认值 0** |

即：**「禁止评论 / 仅我关注的人可评论」属用户主动设置的少数状态**，公开随机抽样命中率低于 0.3%，因此无法靠抽样拿到真实样本来复现 500。

### 3.2 机制解释（含未复现声明）

有直接证据支持的部分：

1. 服务端明明下发了 `can_comment`，而官方客户端**在 false 时直接 return、不发 POST** → 官方 App 不会触发该端点的异常分支。
2. 本客户端没有校验，用户点「发送」后 `POST api/v2/comments` 直达服务端 → 服务端在"不允许评论"分支上抛未捕获异常，返回 500。这与既有结论的模式一致：`DELETE api/v2/comment_votes` 对不存在的 comment_id 同样返回 **500（Rails 未捕获异常）**——该服务端在异常分支上不做友好化处理，500 在这里等于"语义性拒绝"，不是端点损坏。

属**推断、未直接观测**的部分：`no_comments` / `followings_comments` 时 `can_comment` 的确切取值（无样本、且仅一个登录账号，无法以"非作者"身份向受限动态发评论）。

### 3.3 客户端把 500 原样展示给用户

`api_client.dart:227-233` 对 5xx 统一抛 `服务端错误（500）。`，`post_detail_page.dart:179-184` 直接 SnackBar 透传该文案 ⇒ 用户看到的就是这句。这与你观察到的现象吻合。

---

## 四、客户端现状与改动落点

### 4.1 现状

| 位置 | 现状 |
|---|---|
| `api/models.dart:459/547` | 已解析 `commentPermission: asString(j['comment_permission'])`，但**全项目无任何使用点**（仅此 3 处声明/赋值） |
| `api/models.dart` Post | **无** `canComment` |
| `api/simple_api.dart` | **无**单条动态方法（`GET v2/posts/{id}` 从未被调用） |
| `ui/post_detail_page.dart:133-193` `_sendComment` | 无任何权限校验，直接调 `createComment` / `replyComment` |
| `ui/post_detail_page.dart:913-965` 评论栏 | 输入框 + 表情 + 发送键，恒可用 |
| 详情页数据来源 | `widget.post`（列表传入对象）→ 只有 `comment_permission`，**没有** `can_comment` |

### 4.2 改动建议（三个层次，按性价比排序）

1. **详情页做权威判定（核心）**
   - `Post` 增加 `bool? canComment`（`asBool` 解析 `can_comment`，列表来源恒为 null）。
   - `simple_api.dart` 新增 `fetchPost(postId)` → `GET api/v2/posts/{id}`。
   - 详情页进入时（或评论栏首次聚焦时）拉一次单条，拿到权威 `can_comment` 与最新 `comment_permission`；建议与评论列表请求并行，不额外增加等待感。
   - `can_comment == false`：输入框禁用/替换为「作者已开启禁止评论」文案（沿用官方措辞，不暴露端点），并隐藏发送键。

2. **提交前二次校验（防御）**
   - 即便 UI 状态过期，`_sendComment` 也应在服务端权威值为 false 时直接返回提示，避免撞 500。

3. **列表页弱提示（可选）**
   - 列表只有 `comment_permission`，可用 `!= public_comments` 做轻量标记（如评论图标置灰）；因 `comment_permission` 与"当前用户可否评论"并非等价（如 `followings_comments` 取决于是否关注），**不建议**在列表直接禁用互动，只做视觉弱化。
   - ⚠️ 列表端点无 `can_comment`，若要用权威值只能进详情页后补拉。

### 4.3 兜底文案（独立于上述改造）

`POST api/v2/comments` 的 500 目前显示为「服务端错误（500）。」，对用户不可理解。若担心校验有漏网场景，可对该端点的 500 单独映射为「该动态不允许评论」。

---

## 五、可选的确定性验证（需你确认）

在只有一个登录账号的前提下，我能做的**可逆**实验仅限作者视角：

1. `PUT api/v2/posts/{id}` 把自己的某条动态临时设为 `comment_permission=no_comments`；
2. `GET api/v2/posts/{id}` 观察作者视角的 `can_comment`；
3. 以作者身份尝试发一条测试评论，看返回码（作者通常仍可评论，可据此判断 `can_comment` 是"按人计算"还是"仅映射枚举"）；
4. 删除测试评论（若有），并把 `comment_permission` **恢复为原值**，复核。

它**不能**复现"非作者被拒"的 500，但能确认枚举与布尔的联动关系、以及 `PUT api/v2/posts/{id}` 这条写路径是否可用。需要你确认后我才执行（默认不做，本次未对你的账号做任何写操作）。

---

## 附：探查留痕

- 静态提取脚本：`analysis/probe_bundle_comment.py`、`probe_bundle_comment2.py`、`probe_bundle_comment3.py`
  （读本地缓存的官方 Web 产物 `.workbuddy/cache/main.dart.js`，不联网）
- 在线只读脚本：`analysis/probe_comment_perm_live.py`、`live2.py`、`live3.py`、`live4.py`
  （输出 `*_out.txt`：字段存在性、枚举分布、详情端点核对、端点负面基准）
- 纪律：串行 ≥1.25s、浏览器 UA、Authorization=裸 token、不碰 /auths、无并发、无全量抓取、输出不含 token。
- **本次未产生任何写操作**（live3 因未找到 `can_comment=False` 目标，按设计跳过了写探测）。
