# 四需求可行性探查报告（官方 Web 产物 + 带会话实测）

> 探针时间：2026-09-16 · 账号：`15e75816-…`（昵称"难填"）
> 方法：① 从官方 Web 产物 `main.dart.js`（4,338,115 B / md5 `7a173bf608d4deeeeb84f7a0fdc0868f`）
> 静态枚举接口面；② 用真实 token 串行实测（≥1.2s/请求、浏览器 UA、裸 token）。
> 二者缺一不可：产物定"客户端会怎么调"，实测定"服务端到底认不认"。
> 探针脚本：`.workbuddy/tmp/probe_util.py`、`p1~p6_*.py`（可复跑）。

---

## 一、结论速览

| # | 需求 | 结论 | 一句话 |
| --- | --- | --- | --- |
| 1 | 进入他人个人主页 | ✅ **可行** | `GET api/v2/users/{id}` 拿主页头 + `posts/profile` 拿动态流 + `post_collections` 拿合集；**粉丝/关注列表拿不到他人的** |
| 2 | 评论区点赞与回复 | ✅ **全通** | 评论点赞/取消、发表评论、发回复、**读回复列表**、楼中楼、删评论，全部实测 2xx |
| 3 | 获取当前动态链接 | ✅ **可行** | 官方格式 `https://simple.imsummer.cn/sharePost?id=<postId>`；App 深链 `simple://sharePost?id=<postId>` |
| 4 | 当前登录用户的表情包 | ✅ **可行** | 系统表情包 5 套 / 单包 44 张 / **我的表情 53 张**，收藏与取消收藏也已实测闭环 |

四个需求都"能做"，但**各有边界**：1 的"他人粉丝/关注列表"、2 的"他人评论点赞数"、
4 的"上传新表情"是缺口，见下文。

---

## 二、需求 1：他人个人主页

### 2.1 可用端点（实测）

| 用途 | 端点 | 实测 |
| --- | --- | --- |
| 主页头部 | `GET api/v2/users/{id}` | **200 / 857 B** |
| 他人动态流 | `GET api/v2/posts/profile?user_id={id}&per_page=10&last_id=` | **200 / 10 条**（含 `is_pinned`） |
| 他人合集 | `GET api/v2/post_collections?user_id={id}` | **200 / 3 条** |
| 关注 / 取关 | `POST api/v2/follows` `{user_id}` ／ `DELETE api/v2/follows?user_id=` | 假 id → 404 `user.not_found` ⇒ **路由与参数成立** |
| 静音 / 屏蔽 / 反向静音 | `POST api/v2/mutes｜blocks｜reverse_mutes` `{user_id}` | 同上，路由成立 |
| 用户徽章 | `GET api/v2/user_badges?user_id={id}` | ⚠️ 200 但 **`user_id` 被忽略**，返回自己的徽章 |
| 粉丝 / 关注列表 | `GET api/v2/follows/followers｜followings?user_id={id}` | ❌ 200 但 **`user_id` 被忽略**，只返回自己的 |
| 用户子资源 | `GET api/v2/users/{id}/posts` | ❌ 不存在（Rails HTML 404） |

**"他人主页"这一步没有阻碍**：动态对象自带 `user.id`，点作者头像即可 `users/{id}` + `posts/profile`，官方 App 的用户详情页控制器（产物 `A.Ar.kn()`）也正是**同时发这两个请求**。

### 2.2 他人资料能拿到什么（实测字段，857 B）

```
id / avatar_color / avatar_id / nickname / bio / birthday / constellation
simple_id / im_id / level / avatar_url / badges_count / created_at
is_locked / is_inactive / is_official / is_disable_follow / can_private_message
private_message_permission / is_hide_gender_age
is_following / is_follower / is_friend / is_muted / is_reverse_muted / is_blocked
```

**关系态齐全**（`is_following / is_follower / is_friend / is_muted / is_reverse_muted / is_blocked`），
主页可以直接渲染"已关注 / 互关 / 已拉黑"按钮状态，无需额外请求。

> ⚠️ **一处隐私口径要注意**：`is_hide_gender_age=true` 的用户（实测"小狐狸"）响应里
> **确实没有 `gender` / `age`**，但**`birthday` 与 `constellation` 仍然原样下发**
> （`"birthday":"1988-01-06"`）。也就是说年龄性别藏了、生日没藏。
> 客户端推荐不展示 birthday，避免把服务端的隐私漏洞显式呈现在界面上。

### 2.3 两个"静默忽略"陷阱（本次新发现，必须记住）

**A/B 对照实验**（四种输入 + 交错基线）：

| 请求 | 响应长度 | 结果 |
| --- | --- | --- |
| `follows/followers?user_id=<我>` | 3134 B | 完全相同 |
| `follows/followers?user_id=<小狐狸>` | 3134 B | 完全相同 |
| `follows/followers?user_id=<全 0 假 uuid>` | 3134 B | 完全相同 |
| `follows/followers`（完全不传） | 3134 B | 完全相同 |

首条同时是 `c9d312ed-…`，`is_following=false, is_follower=true`。`followings` 同理（3203 B × 4 种输入一致）。
⇒ **`user_id` 在这两个端点上完全没被采纳**，服务端只实现了"当前登录用户的粉丝/关注列表"。
传任何 id（他人的、假的、不传）都返回同一份 —— 所以**"看某人的粉丝列表"这件事做不到**。

`user_badges` 同样忽略 `user_id`，判定证据是**数值自证**：假 id 返回 `badges_count: 13`，
而 `current_user` 的 `badges_count` 正是 **13**，真实他人（小狐狸）是 **17**。

> 这两个端点对客户端的含义：**别把它们挂到他人主页上**，会显示成"我的粉丝"却标着他人的名字。
> 真要做"谁的粉丝"，只能靠 `users/{id}` 的 `is_follower` 逐条判断，没有列表接口。

---

## 三、需求 2：评论点赞与回复

### 3.1 实测结果：**全部可行**（含真写操作闭环）

| 能力 | 端点 | 方法/参数 | 实测 |
| --- | --- | --- | --- |
| 发表评论 | `api/v2/comments` | POST `{content, post_id}` | **201**，返回新评论对象 |
| **评论点赞** | `api/v2/comment_votes` | POST `{comment_id}` | **201**，响应 `is_voted:true` |
| **取消点赞** | `api/v2/comment_votes` | DELETE `{comment_id}` | **200**，响应 `is_voted:false` |
| **发回复** | `api/v2/comments/replies` | POST `{comment_id, content}` | **201**，返回新回复 |
| **回复列表** | `api/v2/comments/replies` | GET `?comment_id=&per_page=&last_id=` | **200**，返回数组 |
| 回复也可点赞 | `api/v2/comment_votes` | POST `{comment_id: <回复id>}` | **201** |
| 楼中楼 | `api/v2/comments/replies` | POST `{comment_id: <回复id>, content}` | **201**（服务端仍平铺一层，用 `replied_user` 表达对象） |
| 删除评论 | `api/v2/comments/{id}` | DELETE | **200** `{"id":"…"}` |
| 置顶评论 | `api/v2/comments/{id}` | PATCH `{is_pinned}` | 产物静态证据（未实测写） |

**闭环验证过程**（全部落在自己动态 `863bbbfe-…` 下，逐条可回收）：

```
发评论 201 → 点赞 201（is_voted=true）→ 取消 200（is_voted=false）
→ 回复 201 → 读回复列表 n=1 → 对回复点赞 201（is_voted=true）→ 取消 200
→ 对回复再回复 201 → 删主评论 200 → 复查该动态评论 []  ✅ 残留 0
```

**跨用户验证（两项都用了真实的"他人"目标）**：

- **点赞他人评论**：对**他人动态**（`cb82b0d5-…`，小狐狸）下的**他人评论** `2253c636-…`（毛茸茸）
  执行 `POST 201 → is_voted=true → DELETE 200 → is_voted=false`，已还原 ⇒ **点赞不限作者**。
- **回复他人评论**：对**我自己的动态**（`6e89d1c4-…`）下**他人发的评论** `2c8b60e5-…`（秋奈澄）
  执行 `POST replies 201 → 读回复列表确认落位 → DELETE comments/{回复id} 200 → 残留 0`，已还原
  ⇒ **回复同样不限作者**；且**单条回复可独立删除**（不必级联删主评论）。
  细节：POST 响应里的 `replied_user` 为 `null`（服务端未回填），
  要展示"回复 @某人"得用本地已知的父评论作者。

### 3.2 必须更正一条旧结论

2026-09-15 的笔记（依据 `replies_count`/`preview_replies` 字段推断）写的是
"**全 app 只有发回复的 POST，没有单独的回复列表端点**"。
**本次实测推翻**：`GET api/v2/comments/replies?comment_id=&per_page=&last_id=` → 200 返回回复数组。
（官方产物里这个 GET 出现 3 次，带 `per_page:"3"` 与 `last_id` 游标，正是评论区的"查看更多回复"。）

### 3.3 ⚠️ 字段陷阱：`votes_count` **只对"自己的评论"下发**（已用控制变量实验钉死）

键名就是 **`votes_count`**。它在响应里**是否出现，取决于 `is_owner`，与赞数无关**：

| 实验组 | `is_owner` | 赞数状态 | `votes_count` | 该键是否存在 |
| --- | --- | --- | --- | --- |
| **自己的评论**（我发的） | true | 0 赞 | **0** | ✅ 存在 |
| 同上 | true | 1 赞 | 1 | ✅ 存在 |
| 同上 | true | 取消回 0 赞 | **0** | ✅ 存在 |
| **他人评论**（小狐狸动态下 3 条） | false | 0 赞 | — | ❌ **不存在** |
| 他人评论（我给它点了赞） | false | 1 赞（`is_voted:true`） | — | ❌ **不存在** |

> 附：`POST/DELETE comment_votes` 的**写响应本体**里也带 `votes_count`（自己评论 = 1 / 0）。

**规则**：`votes_count` 是**"我的评论"专属字段**；他人评论的点赞数**从来不返回**。

**对实现的两个硬影响**：
1. 他人评论**做不了"点赞数"**，只能做"已赞/未赞"状态（靠 `is_voted`）——
   官方的评论点赞本来就是一个纯状态按钮，不是计数器。
2. 解析仍需容错：`json['votes_count'] ?? 0`，因为他人评论整键缺失。

评论对象完整 keys（自己的评论）：
`content / created_at / id / is_author / is_owner / is_pinned / is_voted / replies_count / user / votes_count`；
他人评论则**少最后一项**。

> 更正说明：本报告 15:4x 版曾写"0 赞时服务端省略该键"，那是**只测了
> 「0 赞他人评论」与「1 赞自己评论」两个样本、同时换了两个变量**得出的错误结论。
> 补做的控制变量实验（同一条自己评论走 0 赞 → 1 赞 → 0 赞）证明真实规则是"按 `is_owner` 下发"。
>
> 旁证（未做对照，仅记录观察）：`posts/mine` 返回的自己的动态带 `is_owner:true` 与
> `votes_count`（实测某条 = 42），与"本人的对象才给计数"这条规律一致。

---

## 四、需求 3：获取当前动态链接 / 应用深链

### 4.1 官方链接格式（产物 `bEL()` 是唯一的链接构造器）

| 类型 | 官方分享链接 | App 深链 |
| --- | --- | --- |
| 动态 | `https://simple.imsummer.cn/sharePost?id=<postId>` | `simple://sharePost?id=<postId>` |
| 用户主页 | `https://simple.imsummer.cn/shareFriend?id=<userId>` | `simple://shareFriend?id=<userId>` |
| 合集 | `https://simple.imsummer.cn/shareCollection?user=<uid>&id=<cid>` | `simple://shareCollection?user=…&id=…` |

产物里 `bEL` 的原始形态：`A.au()+"sharePost?"+b`，调用处 `A.bEL(0,"id="+id)` 拼完直接写剪贴板（`A.i5(new A.fO(s+" "))`）。
`A.au()` = `https://simple.imsummer.cn/`。

### 4.2 `sharePost` 跳转页的行为（实测 3486 B，**不校验 id**）

真实 id 与全 0 假 uuid 返回**等长同构**页面 —— 它只是一张"唤起 App"的降落页：

```js
scheme_ios   = 'https://simple.imsummer.cn/sharePost?id=' + id;   // iOS：通用链接
scheme_adr   = 'simple://sharePost?id=' + id;                     // Android：自定义 scheme
download_url_adr = "https://simple.imsummer.cn/m/index.html";      // Android 3 秒回落
download_url_ios = "https://itunes.apple.com/cn/app/id6714463207"; // iOS 3 秒回落
// Android → location.href = scheme_adr；iOS → scheme_ios；微信内 → 只显示"用浏览器打开"提示图
```

### 4.3 深链分发规则（产物 `cad()`，**这是能否被唤起的关键**）

```dart
// 先剥掉 \w+://[^/]+/web*# 前缀
switch (scheme) {
  case "http": case "https":  路由 = path.toLowerCase();       // .../sharePost?id= → "/sharepost"
  case "simple":  host=="app" ? path : "/" + host;             // 两种写法都能命中
}
路由表查找（全部小写比对），命中才执行
```

路由表共 18 条，与主页/动态相关的是：
`/UserDetailPage`、`/MomentDetailPage`、`/sharePost`、`/shareFriend`、`/shareChannel`、
`/MomentNotificationPage`、`/UserBadgesDetailPage`、`/FollowAndFriendPage`、`/HomeChannel` …

⇒ 能唤醒官方 App 的写法有两种，都成立：
`simple://sharePost?id=<id>`（host 当路由名）与 `simple://app/sharePost?id=<id>`（host=app，path 当路由名）。

### 4.4 我们客户端该怎么做（建议）

1. **生成链接**：直接复用官方格式 `https://simple.imsummer.cn/sharePost?id=<postId>`。
   对方装了官方 App → 直接进动态；没装 → 落到下载页。零成本，且与官方分享出去的链接完全一致。
2. **自己收深链**：注册**自己的** scheme（如 `simplesousuo://post?id=<postId>`）。
   **不要注册 `simple://`** —— 会和官方 App 抢同一 scheme，命中谁由系统决定，不可控。
3. 若要做"复制动态链接"按钮，服务端**不需要任何接口**：postId 本地就有，链接是纯字符串拼接。

---

## 五、需求 4：当前登录用户的表情包

### 5.1 三档能力（全部实测）

| 能力 | 端点 | 实测 |
| --- | --- | --- |
| 系统表情包列表 | `GET api/v2/emojis/packages` | **200 / 5 套**，字段 `{id, name, icon}` |
| 某个包的全部表情 | `GET api/v2/emojis?package_emoji_id={pkgId}` | **200 / 44 张**，字段 `{id, url}` |
| **当前用户的表情（收藏）** | `GET api/v2/emojis/favorites` | **200 / 53 张**，字段 `{id, url}` |
| 收藏一张 | `POST api/v2/emojis/favorite` `{user_emoji_id}` | **201**（实测 53 → 54） |
| 取消收藏 | `DELETE api/v2/emojis/favorite` `{ids:[…]}` | **200**（实测回到 53） |
| 置顶 | `POST api/v2/emojis/move_to_front` `{user_emoji_id}` | 产物静态证据 |
| 排序 | `POST api/v2/emojis/sort` `{ids:[…]}` | 产物静态证据 |

5 套系统包：**Simple猫 / 煎蛋表情 / 小黄脸 / 抽象兔 / 小鲸鱼**。

### 5.2 关键细节

- **`emojis` 的 `package_emoji_id` 是必填**：不传 → **400 `{"error":"package_emoji_id is missing"}`（39 B）**；传假 uuid → 404（97 B）。
- **收藏与取消收藏的参数名不一样**（产物里就是这样，实测同样）：
  - 收藏：`{"user_emoji_id": "<emoji id>"}`
  - 取消：`{"ids": ["<emoji id>"]}` —— 传 `user_emoji_id` 会 **400 `{"error":"ids is missing"}`（26 B）**。
- `emojis/favorites` **就是"当前登录用户的表情"**：返回的是七牛上的图片/动图（`/1/emoji/…jpg|gif`），
  即用户在官方端上传/收藏的自定义表情，是一份**账号级数据、与我的 token 绑定**。
- 官方 UI 结构（产物 `A.wx` 控制器）：表情面板只有两个 tab —— `default`（"emoji"，即 5 套系统包）
  与 `collect`（"收藏"，即 `emojis/favorites`）。**没有"我创建的包"这一层**。
- 缺口：**没有上传/新建表情的端点**（枚举到的只有 `favorite / favorites / packages / sort / move_to_front`），
  客户端能做"浏览 + 收藏 + 取消收藏 + 排序"，做不了"新增表情"。

---

## 六、本次新得：错误响应的"判别钥匙"（补进钥匙表）

同一服务端对不同失败给出的响应体**长度与措辞各不相同**，可据此判断是"路由不存在"还是"资源不存在"：

| 响应（长度） | 含义 | 例 |
| --- | --- | --- |
| 34 B `{"status":404,"error":"Not Found"}` | **v3 通用路由不存在** | 任意编造的 v3 路径 |
| 89 B `errors.user.not_found` | 用户域路由命中、用户不存在 | `users/{假id}`、`follows` 假 id |
| **92 B `errors.comment.not_found`** | **评论域路由命中、评论不存在**（本次新增） | `comment_votes`、`comments/replies`、`comments/{id}` 假 id |
| 97 B `errors.postcollection.not_found` | 合集单条路由命中、资源不存在 | `post_collections/{假id}` |
| 97 B `errors.notification.not_found` | 通知路由命中、资源不存在 | `notifications/jump_to_comment?notification_id=假` |
| 26 B `{"error":"ids is missing"}` | 端点命中、**必填参数名就是 `ids`** | `DELETE emojis/favorite` |
| 39 B `{"error":"package_emoji_id is missing"}` | 端点命中、必填参数名是 `package_emoji_id` | `GET emojis` |
| 27 B `{"error":"405 Not Allowed"}` | 路由存在、方法不对 | `GET follows`、`PUT friends/friend_nickname` |
| Rails HTML 404 / 500 / 422 | **没进 API 层**（子资源不存在）或**服务端异常** | `users/{id}/posts` → HTML 404 |

> ⚠️ 两个反直觉案例，别误判：
> 1. `DELETE api/v2/comment_votes` 传**不存在的** comment_id → **500（Rails 错误页）**，
>    而不是 404 —— 服务端取记录时异常没被捕获。对**真实评论**同样调用 → **200**。
>    所以"500"在这里的含义是"资源不存在"，**不代表端点坏**。
> 2. `GET api/v2/comments/replies` 对**不存在**的 comment_id 返回 `[]`（200），
>    不报错 —— 与写端点的行为不一致，列表类接口一律按"空即无"处理。

---

## 七、本次真实写操作清单（可追溯）

| 操作 | 目标 | 结果 | 是否还原 |
| --- | --- | --- | --- |
| 发测试评论 | 自己动态 `863bbbfe-…` | 201 | ✅ 已删（复查 `[]`） |
| 点赞/取消自己评论 | 上述评论 | 201 / 200 | ✅ |
| 发回复 + 楼中楼 | 上述评论 | 201 ×2 | ✅ 随主评论级联删除 |
| 点赞/取消他人评论 | 小狐狸动态下评论 `2253c636-…` | 201 / 200 | ✅ 回到 false |
| 收藏/取消表情 | `Frame 20.png` | 201 / 200 | ✅ 53 → 54 → 53 |
| 关注/静音/屏蔽/提问等 | **一律用全 0 假 uuid** | 404 资源不存在 | — 无副作用 |

除"他人评论一次点赞"会产生一条通知（已撤销）外，未对任何他人数据造成留存改动。

---

## 八、对客户端接入的优先级建议

| 需求 | 成本 | 建议 |
| --- | --- | --- |
| 3 动态链接 | **极低**（纯字符串） | 立刻做：详情页加"复制链接"；注册自有 scheme 做深链 |
| 2 评论点赞 + 回复 | **低**（3 个端点，已有 `Comment` 模型） | 立刻做；读回复改走 `comments/replies` 分页（别只靠 `preview_replies`）；**他人评论只渲染"已赞/未赞"状态、不要渲染点赞数**（服务端不给）；解析用 `votes_count ?? 0` |
| 1 他人主页 | **中**（新页面 + 关系态按钮） | 做；但**粉丝/关注列表必须砍掉**（服务端只给"我的"），关系按钮只用 `users/{id}` 的 `is_following` 等字段 |
| 4 表情 | **中**（表情面板 UI + 图片列表） | 按需做：属于发评论时的输入增强；`emojis/favorites` 可当"我的表情"入口 |

---

## 九、未闭合项（不粉饰）

1. **他人粉丝/关注列表**：只验证了 `user_id` 被忽略、拿不到他人的列表；
   是否存在枚举外的参数名（如 `target_user_id` / `owner_id`）未穷举 —— 已扫到的官方调用里
   该端点**不带 user_id**（只在"我的"场景使用），所以大概率是"没做"而非"换了名字"。
2. **评论点赞数** —— **已闭合**（见 3.3）：控制变量实验证明 `votes_count` 按 `is_owner` 下发、
   与赞数无关。残余不确定只剩两点：① `is_author` 是否也触发（本次样本中
   `is_owner=false, is_author=true` 的"作者在自己动态下的评论"同样无该键，故判断只看 `is_owner`）；
   ② 他人评论的真实赞数有没有别的端点可查（未找到）。
3. **回复列表分页上限**：`per_page=3` 与 `per_page=10` 在只有 3 条回复的样本上返回相同结果，
   未验证大 `per_page` 是否像 `posts/search` 那样有白名单（10/20 正常、50 以上 400）。
   保守起见客户端固定 `per_page=10`。
4. **`PATCH comments/{id}` 置顶**、`emojis/sort`、`emojis/move_to_front` 只有静态证据，未做真实写。
5. **`POST api/v2/questions`** 是本次意外触发（本意是探测用户相关写端点）：
   它接受 `{question_mode, custom_question}`，我用 `{user_id, content}` 调用返回 201，
   读回我的设置仍是原值 `custom` / "有事直说"（**未被改动**），
   且 `GET questions` 同样忽略 `user_id`（与 `follows/followers` 同款陷阱）。
